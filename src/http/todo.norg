*** (?) interseptors
*** (?) pipes
*** 

** (x) the user dtos and validation pipe

** (x) adding the globale guards

** (x) protect all the prisma relations

** (x) type saftey

** (x) try generating the uud

** (x) adding the the media cdn

** (x) findone filters

** (x) dto of a file body


** (x) upload root
** ( ) config module
** (x) profile filters tajination
